import 'package:flutter/material.dart';

import 'teacher_gate.dart';

class TeacherHomeScreen extends StatelessWidget {
  const TeacherHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const TeacherGate();
  }
}

